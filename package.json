{
  "name": "nyc-trip",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/gen-icons.mjs && vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs",
    "encrypt": "node scripts/encrypt-itinerary.mjs"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
